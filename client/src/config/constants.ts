const api_url = "https://pablond-coordination-nightlife-app.glitch.me/api"

export const routes = {
  getBusiness: `${api_url}/business`,
  placeEvent: `${api_url}/rsvp`,
  search: `${api_url}/search`,
  login: `${api_url}/github-cb`,
}
